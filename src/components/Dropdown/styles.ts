import { Dropdown } from 'antd';
import styled from 'styled-components';

export const DropdownCustomer = styled(Dropdown)`
    border: 2px solid #140063;
    width: 30px;
    height: 30px;
    border-radius: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
`;